// index          0        1       2 
const alunos = ['Luiz', 'Maria', 'João']



console.log(typeof alunos) // retorna object
console.log(alunos instanceof Array) // Se for array retorna true;

//alunos.push('Bruna')
//alunos.push('Lucas')

//console.log(alunos.slice(0, 3)) // inicio e final não incluso

//delete alunos[1] // Deleta sem mudar o índice. Retorna undefined

//const removido = alunos.shift() // .shift() remove do inicio do array, o que foi removido pode ser guardado em uma variável.

//const removido = alunos.pop() // .pop() remove do final do array, o que foi removido pode ser guardado em uma variável.

// alunos.unshift('Luiza') // Adiciona no começo do array

// alunos.push('Luiza') // Adiciona no final do array